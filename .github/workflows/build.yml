name: Build Windows Binary

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest
    steps:
      # Check out the repository
      - uses: actions/checkout@v4

      # Set up Ruby environment
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 'head'

      # Install dependencies defined in Gemfile
      - name: Install dependencies
        shell: bash
        run: |
          if [ -f Gemfile ]; then
            bundle install
          else
            echo "No Gemfile found, skipping bundle install."
          fi

      # Install ocra gem
      - name: Install ocra
        run: gem install ocra

      # Build the binary using ocra
      - name: Build Binary
        shell: bash
        run: |
          if [ -f orconverter.rb ]; then
            ocra orconverter.rb --no-autoload --gem-all --dll fiber.so
          else
            echo "Script 'orconverter.rb' not found. Please check the file path."
            exit 1
          fi

      # Optional: Upload the binary as an artifact
      - name: Upload Binary
        uses: actions/upload-artifact@v4
        with:
          name: ORConverter
          path: ./orconverter.exe

